/**
 * ไฟล์เก็บ locator / test id ของทุกหน้าไว้ที่เดียว
 * แก้ที่นี่ที่เดียว เทสทุกไฟล์ที่ import ไปใช้จะอัปเดตตาม
 */
export const locators = {
  catalog: {
    heading: 'catalog-heading',
  },
} as const;
